using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CalculatorEx
{
    public class ParenthesesValidation 
    {
        /*


        public bool Validate(string[] tokens, List<string> parenthesesPair)
        {
            for (int i=0; i<tokens.Length; i++)
            {
                if (tokens[i] == parenthesesPair[0])
                {
                    _parentheses.Push(tokens[i]);
                }
                if (tokens[i] == parenthesesPair[1])
                {
                    if (_parentheses.Count() == 0)
                    {
                        return false;
                    }
                    _parentheses.Pop();
                }
            }
            return true;
        }
        */
    }
    
}
